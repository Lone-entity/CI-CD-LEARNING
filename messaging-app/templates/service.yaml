apiVersion: v1
kind: Service
metadata:
  name: {{ include "messaging-app.fullname" . }}
  labels:
    {{- include "messaging-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "messaging-app.selectorLabels" . | nindent 4 }}
